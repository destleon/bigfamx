version: 1
applications:
  - backend:
      phases:
        build:
          commands:
            - pip install -r requirements.txt
            - chmod +x build_scripts/build.sh
            - ./build_scripts/build.sh
      artifacts:
        baseDirectory: /
        files:
          - '**/*'
      cache:
        paths:
          - /root/.cache/pip